const BASE = 16;

const fonts = {
  family: {
    proximaNova: 'proxima-nova, sans-serif',
    sourceCodePro: 'source-code-pro, monospace',
  },
  size: {
    mobile: BASE,
    desktop: BASE * 1.125,
  },
};

export default fonts;
export { BASE };
